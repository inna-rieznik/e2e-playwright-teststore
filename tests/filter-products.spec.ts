import test from "@playwright/test";

//via api set filters, then check ui
test('[E2E-004] should filter product by composition', async ({ page }) => { });


test('[E2E-004] should clear all filters', async ({ page }) => { });
